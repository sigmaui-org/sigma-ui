
import { Canvas, Meta, Controls, Story, Description } from '@storybook/blocks';

import * as ButtonStory from './Button.stories';

<Meta title="Components/Atoms/Button" />

# RheaTable

RheaTable component is a structured element used in user interfaces to display data in a grid format, typically organized into rows and columns. Each row represents a record or item, while each column represents a specific attribute or field of that record.

## Example: Default Table

Below is a practical example demonstrating the placement of RheaTable in the page. RheaTable provides option to render html table and also table data in json format.

<Canvas of={ ButtonStory.Default }></Canvas>
<Controls of={ ButtonStory.Default } />